/*
 * fixedDict.c
 *
 *  Created on: 25/12/2011
 *      Author: piranna
 */

void main()
{
	ASSERT func == fixedDict_get(&eventmanager_events, event)
					printf("%d %d\n",dict->length,capacity);

	printf("eventmanager_attach: %s = 0x%x, 0x%x (%d)\n",event,func,
			fixedDict_get(&eventmanager_events, event), eventmanager_events.length);

	int i = 0;
	for(; i < 5; i++)
		printf("\t'%s' = 0x%x\n",eventmanager_events.pairs[i].key,
								eventmanager_events.pairs[i].value);

	printf("eventmanager_irq_handler: 0x%x 0x%x\n",&keyboard_handler,
			fixedDict_get(&eventmanager_events, "IRQ/1"));
}


strcmp
printf("%d %s %s\n",cmp, dict->pairs[i].key, key);
